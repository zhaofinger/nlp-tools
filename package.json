{
  "name": "nlp-tools",
  "version": "0.0.1-alpha.6",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "license": "MIT",
  "keywords": [
    "NLP",
    "地址解析"
  ],
  "publishConfig": {
    "registry": "https://registry.npmjs.org/"
  },
  "repository": {
    "type": "git",
    "url": "git@github.com:zhaofinger/nlp-tools.git"
  },
  "dependencies": {
    "nodejieba": "^2.5.1"
  },
  "devDependencies": {
    "@types/node": "^14.14.10",
    "parcel": "^2.0.0-beta.1",
    "ts-node-dev": "^1.0.0",
    "typescript": "^4.1.2"
  },
  "scripts": {
    "start": "ts-node-dev --respawn --transpile-only ./demo/index.ts",
    "build": "rm -rf ./dist && parcel build ./src/index.ts --dist-dir ./dist --no-source-maps",
    "prepublishOnly": "npm run build"
  }
}
